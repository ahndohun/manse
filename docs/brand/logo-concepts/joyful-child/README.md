# Manse joyful-child logo concepts

Generated on 2026-07-21 with the built-in OpenAI image generation tool. Each PNG was produced by an independent generation call.

## Shared prompt

Create a production-grade custom wordmark for `manse`, an open-source active-play game engine. Merge an abstract, non-identifiable child jumping with both arms raised in a joyful “manse!” celebration into the first lowercase `m`, followed immediately by `anse`. Use a warm cream background with charcoal, coral, lime, and optional yellow; keep the result flat, vector-like, rounded, playful, and legible at small sizes. Render the exact word `manse` once with no separate icon, duplicate `m`, detailed face, photorealism, gradients, texture, mockup, 3D, shadow, watermark, or extra text.

## Concept prompts

1. Star-jump `m`: raised arms become the arches and bent jumping legs become the lower terminals.
2. Negative-space child: carve the celebrating child from a bold rounded `m`.
3. Motion-trail `m`: the child's raised-arm motion trail draws the two arches.
4. Bouncy monoline: use one continuous line for the jumping child and first `m`.
5. Pose-node joy: use a lime head dot and minimal tracking-style limb segments.
6. Paper-cut leap: use two broad rounded bands for uplifted arms and kicking legs.
7. Playground spring: compress the entry stroke and launch the child upward from the `m`.
8. Ribbon child: use a soft ribbon for the skipping child and `m` simultaneously.
9. Compact badge `m`: merge the body into the central valley and imply airborne feet with base cuts.
10. Celebration endpoints: terminate the uplifted arms with exactly two lime hand dots.

## Provenance and license

- Generation method: built-in OpenAI image generation tool.
- Third-party assets: none.
- Status: exploratory brand concepts, not yet selected as the production logo.
- Intended project license after selection: MIT, consistent with the Manse repository. Confirm and redraw the selected production vector before release.
